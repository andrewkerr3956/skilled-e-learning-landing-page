const Footer = (
    <footer className="w-full bg-navy">
        <div className="container flex flex-1 justify-between items-center gap-2 sm:gap-0 flex-wrap sm:flex-nowrap text-white py-[32px]">
            <img src="/assets/logo-light.svg" alt="Skilled Logo" className="block" width={111} />
            <button type="button" className="p-btn rounded-btn text-white bg-blue-gradient">Get Started</button>
        </div>
    </footer>
);

export default Footer;